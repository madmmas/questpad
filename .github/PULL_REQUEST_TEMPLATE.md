## What this changes

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run test` passes
- [ ] `npm run test:e2e` passes locally (if a user-facing flow changed)
- [ ] No real family data, images, or secrets included
